use crate::world::World;

use super::App;

impl App {
    pub fn update(&mut self, world_buf: &World, idx: usize) {
        match world_buf.segments[idx] {
            
            _ => {}
        }
    }
}